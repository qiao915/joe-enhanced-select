1. 概述

JoeEnhancedSelect 是一个增强型 React 下拉选择组件，专为支持 模糊搜索 + 精确匹配优先 + 匹配文本高亮 的业务场景设计。适用于商品品牌、标签、分类等需要智能筛选的输入场景。

该组件将作为独立 npm 包发布（如 @your-org/joe-enhanced-select），支持在函数组件和类组件中使用，具备良好的扩展性和样式隔离能力。

2. 核心功能

- ✅ 支持实时模糊搜索（含精确匹配优先排序）
- ✅ 匹配文本高亮显示，颜色可自定义（默认：高亮色 #0066cc，正常色 #333333）
- ✅ 支持单选 / 多选模式（通过 multiple 配置项控制，默认单选）
- ✅ 支持异步加载选项（通过 loadOptions 函数）
- ✅ 自动处理“无结果”状态，显示友好提示
- ✅ 不依赖原生 ，完全自定义 UI
- ✅ 无需键盘导航支持（简化交互）
- ✅ 样式类名带前缀 joe-enhanced-select-，避免全局冲突
- ✅ 兼容 React 函数组件 & 类组件
- ✅ 自动管理异步 loading 状态（用户无需手动传 loading）

⏳ 第二期规划：虚拟滚动、选项分组（本期暂不实现）

3. Props 定义（TypeScript 接口）
```typescript
interface Option {
  value: string | number;
  label: string;
  [key: string]: any; // 允许扩展字段
}

interface JoeEnhancedSelectProps {
  // 基础配置
  options?: Option[];                     // 静态选项列表（与 loadOptions 二选一）
  value?: string | number | (string | number)[] | null; // 当前选中值
  onChange: (value: string | number | (string | number)[] | null) => void;

  // 行为控制
  multiple?: boolean;                     // 是否多选，默认 false
  disabled?: boolean;                     // 是否禁用，默认 false
  placeholder?: string;                   // 输入框占位符
  noResultsText?: string;                 // 无结果提示，默认 "暂无数据"
  searchPlaceholder?: string;             // 搜索框提示，默认同 placeholder

  // 异步加载（推荐方式）
  loadOptions?: (inputValue: string) => Promise; // 异步获取选项

  // 高级控制（可选）
  loadingText?: string;                      // 【可选】自定义加载中提示，默认 "加载中..."

  // 高亮颜色自定义
  highlightColor?: string;                // 匹配文字高亮颜色，默认 "#0066cc"
  normalTextColor?: string;               // 正常文本颜色，默认 "#333333"

  // 扩展
  className?: string;                     // 外部根容器类名
  style?: React.CSSProperties;            // 外部样式
}
```

💡 关键说明：
- 若提供 loadOptions，组件将：
- 在用户输入时自动调用（带 300ms 防抖）；
- 自动设置内部 isLoading = true/false；
- 显示“加载中…”提示；
- 用户通常不需要传 loading，仅在特殊场景（如手动触发刷新）时使用。

4. 匹配与排序逻辑

4.1 匹配规则（按优先级排序）
|优先级|匹配类型     |判断条件|
|:---:|:---:|:---:|
|1       | 精确匹配         |option.label === query|
|2        |前缀匹配         |option.label.startsWith(query)|
|3        |包含匹配         |option.label.includes(query)|

所有匹配项按上述顺序排列，非匹配项不显示。

4.2 高亮实现

- 使用正则表达式提取所有匹配片段（忽略大小写）；
- 将匹配部分包裹在  中，并应用 color: highlightColor；
- 非匹配部分使用 normalTextColor；

5. 异步加载逻辑（组件内部管理）

- 用户输入 → 触发 loadOptions(inputValue)（防抖 300ms）；
- 调用期间，组件内部 isLoading = true；
- 下拉面板显示 .joe-enhanced-select__loading 元素（文本：“加载中…”）；
- 请求完成 → 更新选项列表，isLoading = false；
- 若 loadOptions 抛出异常，应降级为“暂无数据”并记录警告（不崩溃）；

6. 多选模式行为

- 选中项以标签形式展示在输入框内；
- 点击标签 × 可删除（触发 onChange 移除对应值）；
- 下拉列表中点击已选项可取消（多选）；
- onChange 返回数组（如 ['a', 'b']）；

7. CSS 类名规范（带前缀）
|元素|类名|
|:---:|:---:|
|根容器|.joe-enhanced-select|
|输入框|.joe-enhanced-select__input|
|下拉面板|.joe-enhanced-select__menu|
|选项项|.joe-enhanced-select__option|
|已选项标签（多选）|.joe-enhanced-select__tag|
|删除按钮|.joe-enhanced-select__tag-remove|
|高亮文本|.joe-enhanced-select__highlight|
|无结果提示|.joe-enhanced-select__no-results|
|加载中提示|.joe-enhanced-select__loading|
|输入框|.joe-enhanced-select__input|
|下拉面板|.joe-enhanced-select__menu|
|选项项|.joe-enhanced-select__option|
|已选项标签（多选）|.joe-enhanced-select__tag|
|删除按钮|.joe-enhanced-select__tag-remove|
|高亮文本|.joe-enhanced-select__highlight|
|无结果提示|.joe-enhanced-select__no-results|
|加载中提示|.joe-enhanced-select__loading|
|禁用状态|.joe-enhanced-select--disabled|

8. 使用示例（全部修正）

8.1 函数组件：基础单选
```typescript
import React, { useState } from 'react';
import { JoeEnhancedSelect } from '@your-org/joe-enhanced-select';

const options = [
  { value: 'mc', label: '名创优品' },
  { value: 'ml', label: '名龙堂' },
];

export default function SingleSelectExample() {
  const [value, setValue] = useState(null);

  return (
     setValue(v as string | null)}
      placeholder="请选择品牌"
    />
  );
}
```

8.2 函数组件：异步单选
```typescript
import React, { useState } from 'react';
import { JoeEnhancedSelect, Option } from '@your-org/joe-enhanced-select';

async function loadBrands(query: string): Promise {
  if (!query.trim()) return [];
  await new Promise((r) => setTimeout(r, 400)); // 模拟网络延迟
  return [{ value: 'brand-' + query, label: 品牌-{query} }];
}

export default function AsyncSingleSelect() {
  const [value, setValue] = useState(null);

  return (
     setValue(v as string | null)}
      placeholder="异步搜索品牌（单选）"
    />
  );
}
```

✅ 用户无需关心 loading 状态，组件自动处理。

8.3 函数组件：异步多选
```typescript
import React, { useState } from 'react';
import { JoeEnhancedSelect, Option } from '@your-org/joe-enhanced-select';

async function loadTags(query: string): Promise {
  if (!query.trim()) return [];
  await new Promise((r) => setTimeout(r, 300));
  return [
    { value: 't1', label: {query}-标签1 },
    { value: 't2', label: {query}-标签2 },
  ];
}

export default function AsyncMultiSelect() {
  const [value, setValue] = useState([]);

  return (
     setValue(Array.isArray(v) ? v : [])}
      placeholder="异步搜索标签（多选）"
    />
  );
}
```

8.4 类组件：基础单选
```typescript
import React from 'react';
import { JoeEnhancedSelect, Option } from '@your-org/joe-enhanced-select';

const staticOptions: Option[] = [
  { value: 'a', label: '选项A' },
  { value: 'b', label: '选项B' },
];

export class ClassSingleSelect extends React.Component {
  state = { value: null as string | null };

  handleChange = (newValue: any) => {
    this.setState({ value: newValue as string | null });
  };

  render() {
    return (
      
    );
  }
}
```

8.5 类组件：异步单选
```typescript
import React from 'react';
import { JoeEnhancedSelect, Option } from '@your-org/joe-enhanced-select';

export class ClassAsyncSingle extends React.Component {
  state = { value: null as string | null };

  // 使用箭头函数确保 this 绑定
  loadOptions = async (query: string): Promise => {
    if (!query.trim()) return [];
    await new Promise((r) => setTimeout(r, 500));
    return [{ value: 'async-' + query, label: 异步-{query} }];
  };

  handleChange = (newValue: any) => {
    this.setState({ value: newValue as string | null });
  };

  render() {
    return (
      
    );
  }
}
```

8.6 类组件：异步多选
```typescript
import React from 'react';
import { JoeEnhancedSelect, Option } from '@your-org/joe-enhanced-select';

export class ClassAsyncMulti extends React.Component {
  state = { value: [] as (string | number)[] };

  loadOptions = async (query: string): Promise => {
    if (!query.trim()) return [];
    await new Promise((r) => setTimeout(r, 400));
    return [
      { value: 'm1', label: {query}-多选1 },
      { value: 'm2', label: {query}-多选2 },
    ];
  };

  handleChange = (newValue: any) => {
    this.setState({
      value: Array.isArray(newValue) ? newValue : [],
    });
  };

  render() {
    return (
      
    );
  }
}
```

✅ 所有类组件示例中，loadOptions 为实例方法（箭头函数），避免 this 丢失。

9. 技术栈与构建

|项目|要求|
|:--:|:--:|
|React 版本|≥ 16.8|
|构建工具|Vite（vite build --lib）|
|语言|TypeScript|
|样式|CSS Modules（scoped）|
|测试|Jest + React Testing Library|
|文档|Storybook|
|输出格式|ESM + CommonJS，带 .d.ts|

10. 交付物

1. ✅ JoeEnhancedSelect 组件（TSX）
2. ✅ 类型定义文件
3. ✅ 单元测试（覆盖同步/异步/单选/多选）
4. ✅ Storybook 示例
5. ✅ README.md
6. ✅ 支持 Tree-shaking 和 SSR

11. 注意事项

- ❌ 不支持键盘导航；
- ✅ 异步 loading 由组件内部管理，用户无需传 loading；
- ✅ 类组件中 loadOptions 必须为箭头函数或正确绑定；
- ✅ 高亮使用 inline style，支持动态颜色；
- ✅ 所有示例均可直接运行，无类型或逻辑错误。

12. 第二期预留

- virtualized?: boolean
- groupBy?: (option: Option) => string
